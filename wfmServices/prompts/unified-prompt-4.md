This is a new feature.

Currently, we have orders screen as attached screenshots. But this doesnt give a wholistic picture of how many orders are there and each order has how many processes and each process has how many tasks. What is the status of each process and how many tasks are completed in each process and how many tasks are pending ? Tab format is NOT looking good.

Can you please suggest what could be better solution here ? Can we show orders / processes / tasks relation in a better way ? Can we show it in nested tables (orders -> processes -> tasks, ofcourse with lazy loading) ? Or any other better component that you can suggest ?